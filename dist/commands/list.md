---
name: blk:list
description: List all available system agents with descriptions
version: 1.0.0
---

# List Command

Use `run_shell_command` to execute the Python manager script:
`python3 ${extensionPath}/scripts/agent_manager.py list`

Task: Display the output directly to the user.