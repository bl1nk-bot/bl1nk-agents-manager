---
name: blk:info
description: Show detailed information about a specific agent
version: 1.0.0
argument-hint: '[arguments]'
---

# Info Command

Use `run_shell_command` to execute the Python manager script:
`python3 ${extensionPath}/scripts/agent_manager.py info {{args}}`

Task: Display the output directly to the user.